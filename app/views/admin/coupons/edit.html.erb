<%= form_with(model: @coupon, url: admin_coupon_path(@coupon), method: 'patch', local: true) do |f| %>
  <%=render 'form', f: f%>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<%= link_to 'Back', admin_coupons_path %>
