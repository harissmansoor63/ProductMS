<%= form_with(model: @coupon, url: admin_coupons_path, method: 'post', local: true) do |f| %>
  <%=render 'form', f: f%>

  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>

<%= link_to 'Back', admin_coupons_path %>
